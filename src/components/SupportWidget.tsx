import DonationPage from "@/components/DonationPage";

const SupportWidget = () => {
  return (
    <section id="support" className="py-24 md:py-32 px-6 bg-muted/50">
      <DonationPage />
    </section>
  );
};

export default SupportWidget;
